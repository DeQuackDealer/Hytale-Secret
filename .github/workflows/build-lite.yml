name: Build Rubidium Lite Edition

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-lite:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Rubidium-Framework
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Set up JDK 25
      uses: actions/setup-java@v4
      with:
        java-version: '25-ea'
        distribution: 'temurin'
        cache: 'gradle'
    
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    
    - name: Build Lite Edition
      run: ./gradlew clean rubidiumLiteJar --no-daemon
    
    - name: Verify Build
      run: |
        echo "=== Checking Build Output ==="
        ls -la build/libs/
        if [ ! -f "build/libs/rubidium_lite.jar" ]; then
          echo "ERROR: rubidium_lite.jar not found!"
          exit 1
        fi
        echo "Build successful!"
    
    - name: Verify Manifest
      run: |
        echo "=== Manifest Content ==="
        unzip -p build/libs/rubidium_lite.jar manifest.json
    
    - name: Upload Rubidium Lite
      uses: actions/upload-artifact@v4
      with:
        name: rubidium_lite
        path: Rubidium-Framework/build/libs/rubidium_lite.jar
        if-no-files-found: error
